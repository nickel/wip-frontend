---
const { project } = Astro.props
---

<h3 class="text-xl font-extrabold">
  <a href={project.reference_url}>{project.name} →</a>
</h3>

<p class="text-gray-700 mt-2 pl-4">
  {project.description}
</p>

<ul class="pl-4 mt-2 flex flex-wrap items-center space-x-1">
  {project.tags.map(tag => (
    <li class="font-extrabold">#{tag}</li>
  ))}
</ul>
